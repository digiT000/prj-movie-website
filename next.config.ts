import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["seotoolspark.com"], // Replace with your actual image domain(s)
  },
};

export default nextConfig;
