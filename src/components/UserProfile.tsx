import React from "react";

interface UserProfileProps {
  status: string;
}

export default function UserProfile({ status }: UserProfileProps) {
  switch (status) {
    case value:
      break;

    default:
      break;
  }
}
